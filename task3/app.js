// Создаем переменную и сохраняем в ней значение 10 (строка) + 10 (число)
var myVariable = "10" + 10;

// Печатаем тип переменной в консоль
console.log(typeof myVariable);

// Вівод значение переменной в консоль
console.log(myVariable);
